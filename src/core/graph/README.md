# `src/core/graph`

Graph and topology helpers used by the simulation layer.

## Files

- `graph-state.ts` stores the graph model
- `topology.ts` builds graph topologies used by the simulations
- `laplacian.ts` contains Laplacian helpers for graph analysis

## Purpose

The graph code supports the simulation harness and the benchmark scenarios. It is not part of the live node runtime.
